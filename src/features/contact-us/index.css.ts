import { style } from '@vanilla-extract/css'

export const wrapper = style({
  width: '100%',
  padding: '20px'
})

export const inputWrapper = style({
  position: 'relative',
  margin: '20px auto'
})
