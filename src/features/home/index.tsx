import React from 'react'
import HeroImage from '@/components/hero-image'
import Hero from '@/images/home/home.hero.png'

const Home = (): JSX.Element => {
  return <HeroImage src={Hero} />
}

export default Home
